# Linux

Path: `~/.config/Claude-3p/configLibrary/`

Not independently verified on Linux — no Linux machine with Claude Desktop installed was
available for this research. Same confidence rationale as `../windows/README.md`: the schema
comes from the shared Electron JS bundle, only the base directory path is platform-specific, and
that branch was read directly rather than assumed. A real Linux run would still be worth doing
before fully trusting the automated writer on that platform.
